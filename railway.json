{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @spheres/world-server build"
  },
  "deploy": {
    "startCommand": "cd apps/world-server && node dist/index.js"
  }
}
